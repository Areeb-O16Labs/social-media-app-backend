export enum userRoles { USER = 'user', ADMIN = 'admin' }

export enum userStatus { ACTIVE = 'active', INACTIVE = 'inactive', DELETED = 'deleted', PENDING = 'pending' }

export enum postStatus { ACTIVE = 'active', INACTIVE = 'inactive', DELETED = 'deleted', PENDING = 'pending' }